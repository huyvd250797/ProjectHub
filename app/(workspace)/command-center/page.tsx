import { ProjectCommandCenter } from "@/components/command-center/project-command-center";

export const metadata = { title: "Project Command Center" };

export default function CommandCenterPage() {
  return <ProjectCommandCenter />;
}
