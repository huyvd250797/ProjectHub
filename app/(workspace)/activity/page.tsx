import { ActivityCenter } from "@/components/notifications/activity-center";

export const metadata = { title: "Notifications & Activity" };

export default function ActivityPage() {
  return <ActivityCenter />;
}
