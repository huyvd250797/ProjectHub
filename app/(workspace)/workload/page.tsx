import { WorkloadDashboard } from "@/components/workload/workload-dashboard";

export const metadata = { title: "Workload & Capacity Planning" };

export default function WorkloadPage() {
  return <WorkloadDashboard />;
}
