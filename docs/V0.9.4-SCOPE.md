# ASC WORKING V0.9.4 — Searchable Combobox & Sticky Grid UX

## Mục tiêu

Tối ưu tốc độ thao tác ở các danh sách dài trước Production Release, không thay đổi schema hay nghiệp vụ.

## Phạm vi

1. Search input cho `ThemedSelect` dùng trên toàn hệ thống.
2. Search input cho `FloatingSelect` dùng trong lưới ISSUE.
3. Tìm kiếm không dấu, không phân biệt hoa/thường.
4. Tự focus search khi mở combobox.
5. Empty state khi không có option phù hợp.
6. Sticky header cho lưới ISSUE.
7. Giữ tương thích pinned columns và horizontal scroll.

## Không thay đổi

- Database schema.
- RLS / role.
- Excel Import V0.9.2.
- Project Profile V0.9.3.
- ISSUE CRUD/Productivity business rules.
