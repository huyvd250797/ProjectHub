# ASC WORKING V2.4.1 - Validation

## Checklist

- Mở `/workload`, dòng nhân sự không còn render danh sách ISSUE inline.
- Bấm tên nhân sự mở modal full-screen.
- Modal hiển thị đủ ISSUE đang phụ trách.
- Có thể tìm trong modal theo nội dung, module, phòng ban, priority, trạng thái hoặc Jira.
- Jira hiển thị mã như `INTRACOM01-540` và bấm được link.
- Bấm **Đóng** hoặc Esc sẽ tắt modal và vẫn ở màn Workload.
- Workload table vẫn hiển thị Capacity Score, ISSUE count, task, milestone, reminder và khuyến nghị.

## Kiểm tra kỹ thuật

Chạy:

```bash
npm run preflight
npm run typecheck
npm run lint
npm run build
```

## Database

Không cần migration mới.
