# ASC WORKING V1.5.0 — Validation

Ngày kiểm tra: 27/08/2026.

## Kết quả tự động

| Kiểm tra | Kết quả |
|---|---|
| V1.5.0 preflight | 316/316 pass |
| TypeScript `tsc --noEmit` | Pass |
| ESLint toàn source | Pass, 0 error; 105 cảnh báo legacy |
| ESLint nhóm file V1.5.0 | Pass, 0 error |
| Next.js production build | Pass |
| Route generation | Có `/api/workspace/preferences`; các route ISSUE và Documents vẫn được tạo |
| Demo smoke | `/api/health`, `/issues`, `/documents`, `/api/workspace/preferences` đều HTTP 200 |
| Health metadata | Trả `version: 1.5.0` và đủ bốn feature flag cá nhân hóa |

## Kiểm tra an toàn cấu hình

- API màu tag chỉ nhận mã màu hex sáu ký tự, giới hạn số mục và bốn nhóm hợp lệ.
- API navbar chỉ nhận chín route được phép, loại trùng và tự bổ sung module bị thiếu.
- `workspace_user_preferences` dùng RLS theo `auth.uid()`; không cho đọc/ghi preference tài khoản khác.
- ISSUE tag/filter/column preference giữ phạm vi user + Project hiện có.
- Migration chỉ bổ sung preference; không sửa dữ liệu ISSUE, Project Documents hoặc file Google Drive.

## Điều kiện UAT production

Chạy migration V1.5.0 rồi kiểm tra persistence nhiều tài khoản/nhiều Project theo `UAT_V150_ISSUE_PERSONALIZATION_CHECKLIST.md`. Google Drive thật và Supabase RLS production vẫn cần credential/environment của môi trường triển khai.
