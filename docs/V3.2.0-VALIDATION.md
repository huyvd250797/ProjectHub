# ASC WORKING V3.2.0 - Validation

## Kiểm tra nhanh

- Chạy `npm run preflight`; tất cả kiểm tra V3.2.0 phải PASS.
- Chạy migration `202609080002_v320_project_timeline_pro.sql` trên Supabase.
- Mở `/plan`, chọn tab **Timeline Pro**.
- Kiểm tra current plan, baseline, target, milestone và critical stage hiển thị cùng nhau.
- Bấm **Chụp baseline hiện tại**, sau đó kéo một stage sang ngày khác; kiểm tra số ngày trễ và màu cảnh báo.
- Kéo marker deadline của một task; kiểm tra task được cập nhật sau khi tải lại.
- Đăng nhập Viewer để xác nhận không thấy thao tác snapshot/kéo thả.

## Regression

- Master Plan, stage date range, execution task, reminder, financial control và modal scroll lock vẫn hoạt động.
- Demo Mode vẫn hiển thị Timeline Pro read-only.
