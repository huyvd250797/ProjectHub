# V0.5.0 Validation

- Package version: `0.5.0`.
- Department route: `/departments`.
- Department API: `/api/departments?projectId=`.
- Supabase RPC: `get_project_departments(uuid)`.
- V0.5 migration included: `202608220004_v050_department_rpc.sql`.
- No native `<select>` added; filters reuse `ThemedSelect`.
- Department totals include a synthetic unmapped bucket when `issues.department_id is null`.
- Dashboard department links target Department Intelligence.
- Existing PLHĐ V0.4 and Dashboard V0.3 features remain in source.

## Build validation note
- TypeScript/TSX syntax was parsed locally with TypeScript compiler APIs: OK.
- `package.json`, `tsconfig.json`, `vercel.json`: valid JSON.
- Full `npm install && npm run build` could not complete in the artifact environment because npm registry access timed out; Vercel should perform the dependency install from `package.json`.
