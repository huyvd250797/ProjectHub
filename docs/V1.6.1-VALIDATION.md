# ASC WORKING V1.6.1 — Validation

## Automated

Chạy tại thư mục source:

```bash
npm ci
npm run check
```

`check` gồm preflight, TypeScript, ESLint và production build.

## Runtime smoke

- `/api/health` trả `version: 1.6.1` và feature `manual-stage-date-ranges`.
- `/api/plan?projectId=...` trả `dateMode` cho mọi stage.
- `/plan` tải được Overview, Timeline, Project Stages và Milestones.

## Database/UAT

Sau migration V1.6.0, chạy `202609030002_v161_stage_date_range.sql`, sau đó thực hiện toàn bộ `UAT_V161_STAGE_DATE_RANGE_CHECKLIST.md`.
