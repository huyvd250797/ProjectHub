# ASC WORKING V0.9.4 — Validation

- Package version: `0.9.4`
- UAT preflight: `44/44 PASS`
- TypeScript/TSX syntax parse: `93 files OK`
- JSON validation: `package.json`, `tsconfig.json`, `vercel.json` OK
- Native `<select>` in app/components/lib: none
- Shared searchable combobox components:
  - `ThemedSelect`
  - `FloatingSelect`
- ISSUE sticky grid assertions:
  - dedicated vertical/horizontal scroll viewport
  - sticky top header
  - sticky left checkbox/pinned columns
- No database migration required for V0.9.4.

Full dependency install / `next build` was not completed in the artifact environment because npm registry access timed out. Vercel should run the production build after push.
