# ASC WORKING V0.6.0 — Validation

- Package version: `0.6.0`.
- 59 TS/TSX files parsed successfully with TypeScript syntax transpilation.
- `package.json`, `tsconfig.json`, `vercel.json` parsed successfully.
- Required ISSUE Core API/UI/migration files present.
- Existing deep-link keys from Dashboard / PLHĐ / Department Intelligence are supported.
- `.env.local` is not packaged.
- HV logo checksum is unchanged from V0.5.0 source.
- `app/icon.jpg` and `public/branding/hv-logo.jpg` have identical SHA-256.
- Full `npm install && npm run build` could not be completed in the working environment because npm registry access timed out; Vercel should run the normal dependency installation from `package.json`.
