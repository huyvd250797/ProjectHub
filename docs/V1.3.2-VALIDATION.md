# ASC WORKING V1.3.2 — Validation

## File Excel tham chiếu

Đã kiểm tra cấu trúc `IMP-PLHD-PhongBan.xlsx` bằng cách đọc trực tiếp nội dung workbook:

- `Phòng ban`: 9 dòng dữ liệu.
- `PLHĐ`: 119 dòng dữ liệu.
- `PLHĐ chi tiết`: 73 dòng dữ liệu.

Với quy tắc nhận diện của Direct Excel Import, sheet PLHĐ được suy luận thành:

- 1 Nhóm/Root.
- 9 Phân hệ.
- 109 Module.

Với sheet PLHĐ chi tiết:

- 3 node nhóm/phân hệ.
- 70 dòng nghiệp vụ.
- 70/70 dòng nghiệp vụ được mapping vào Module theo cấu trúc/tên của file tham chiếu.

## Source validation

- `npm run preflight`: 246/246 PASS.
- TypeScript syntax transpile: 125 TS/TSX files PASS.
- Full `npm run build` cần dependencies và sẽ được Vercel thực hiện khi deploy.

## Database

Migration V1.3.2 được viết idempotent theo hướng rerun được, nhưng cần chạy trên Supabase của Project để xác nhận schema/runtime thực tế trước UAT Apply Import.
