# ASC WORKING V1.9.0 — Validation

## Automated

Run:

```bash
npm run preflight
npm run typecheck
npm run lint -- --quiet
npm run build
```

Expected:

- Package version là `1.9.0`.
- Health endpoint trả release `Cross-Project Portfolio Dashboard`.
- Preflight tìm thấy `/portfolio`, `/api/portfolio`, portfolio types/demo/component.
- Preflight tìm thấy Project Catalog bulk hard delete.
- Build output có route `/portfolio` và `/api/portfolio`.

## Manual

- Mở `/portfolio`, kiểm tra KPI tổng và Project Ranking.
- Chọn một project trong Priority Board, sau đó mở Dashboard để xác nhận context project đã đổi.
- Vào Danh mục Phòng ban, check nhiều dòng và xóa.
- Vào Danh mục Module, check nhiều dòng và xóa.
- Xác nhận dữ liệu đang được sử dụng không bị xóa.
