# V0.9.0 Validation

## Static validation targets
- package.json version = 0.9.0
- readiness API tồn tại
- UAT page tồn tại
- V0.9.0 migration tồn tại
- `.env.local` và `tsconfig.tsbuildinfo` không nằm trong source package
- Remote Resource list không query permission theo từng row
- Security headers được cấu hình trong next.config.ts

## Runtime validation
1. `npm run preflight`
2. `npm run typecheck`
3. `npm run lint`
4. `npm run build`
5. Vercel Preview deployment
6. `/api/health`
7. `/settings/uat`

## Expected release state
V0.9.0 là Production Candidate, chưa phải final Production Release.
