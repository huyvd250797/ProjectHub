# ASC WORKING V0.8.0 — Validation checklist

- Package version = 0.8.0.
- V0.8.0 migration exists.
- Remote resource list API never selects `secret_ciphertext`.
- Secret table is accessed only through server service-role code.
- AES-256-GCM encryption uses random 12-byte IV + auth tag + AAD.
- Reveal/Copy validates authenticated user, project membership and permission.
- Reveal response uses no-store and UI auto-hides after 10 seconds.
- Copy audit is written without secret content.
- PM/Admin CRUD resource metadata.
- Member/Viewer cannot mutate resource metadata via RLS/API.
- APP_ENCRYPTION_KEY/SERVICE_ROLE are server-only env vars.
