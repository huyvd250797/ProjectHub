# ASC WORKING V1.8.0 — Validation

## Automated

Run:

```bash
npm run preflight
npm run typecheck
npm run lint -- --quiet
npm run build
```

Expected:

- Package version là `1.8.0`.
- Health endpoint trả release `Smart Reminders & Alerts`.
- Preflight tìm thấy route `/api/plan/reminders`.
- Migration V1.8.0 tạo bảng `project_plan_reminders`, RLS, validate trigger, activity trigger và notification sync.
- Planning UI có `Smart Alerts`, `PlanReminderModal`, `Reminder quá hạn`, `Snooze`.

## Manual

- Tạo reminder hôm nay và xác nhận nó hiện trong Smart Alerts.
- Snooze reminder, xác nhận ngày nhắc chuyển sang ngày mới.
- Done reminder, xác nhận biến khỏi danh sách reminder mở.
- Kiểm tra readiness sau khi chạy migration.
