# ASC WORKING V0.9.1 — Validation

## Automated package preflight
`npm run preflight` → **16/16 PASS**.

Validated:
- package version = 0.9.1;
- V0.9.0 hardening migration retained;
- V0.9.1 master migration packaged;
- `promote-master.sql` packaged;
- Master Project Console page/API packaged;
- V0.9.0 + V0.9.1 UAT checklist packaged;
- `.env.local` not packaged;
- `tsconfig.tsbuildinfo` not packaged;
- Supabase/Remote Security environment contract preserved.

## Type/syntax validation
- TypeScript transpile syntax validation completed across all TS/TSX files with **0 syntax diagnostics**.
- Targeted strict type validation was run for V0.9.1 Master/Multi-Project files with temporary external-module stubs; no V0.9.1 business-logic type error remained after fixes.
- Full `npm install && npm run build` could not be completed in the artifact environment because npm registry access timed out. Vercel should run the authoritative production build after push.

## Security assertions
- MASTER access is implemented through `profiles.global_role` + security-definer `is_master()`.
- Existing RLS policies inherit MASTER via upgraded `is_project_member()` / `has_project_role()`.
- User thường still requires `project_members`.
- Non-MASTER self-promotion is blocked by `guard_profile_global_role_trigger`.
- MASTER is mapped to effective Admin in ISSUE and Resource Vault server APIs.
- Remote credential encryption/audit from V0.8.0 remains unchanged.

## UAT cases before V1.0.0
1. Promote primary account to MASTER.
2. Confirm MASTER can access EPU without project_members.
3. Create a second Project from Master Project Console.
4. Confirm Project Switcher shows both Projects for MASTER.
5. Confirm a normal user sees only assigned Project(s).
6. Add/remove a normal user from a Project and verify access changes.
7. Validate Dashboard/PLHĐ/Department/ISSUE/Remote on the second Project.
8. Confirm Remote Reveal/Copy works for MASTER as effective Admin.
9. Attempt to change a normal user's own `global_role` to master and confirm database rejects it.
10. Run Hardening & UAT Center and clear all FAIL items.
