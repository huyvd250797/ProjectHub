# ASC WORKING V1.6.0 — Validation

## Automated

```bash
npm run preflight
npm run typecheck
npm run lint
npm run build
```

## Expected

- Preflight: toàn bộ required files/tokens PASS.
- TypeScript: không có lỗi.
- ESLint: không có error; source baseline có thể còn warning đã biết.
- Next.js production build: thành công và có route `/plan` cùng các API `/api/plan/*`.

## Database

Sau khi chạy migration, dùng **Thiết lập → Hardening & UAT** để xác nhận check `Master Plan & Project Stages` ở trạng thái PASS.

## Manual

Thực hiện toàn bộ `UAT_V160_MASTER_PLAN_CHECKLIST.md` trên ít nhất hai role: một PM và một Viewer/Member.
