# ASC WORKING V0.7.0 — Validation

- Package version: `0.7.0`.
- Source upgraded from V0.6.0 DeployFix.
- Type mismatch fix for `SelectOption.description` is retained.
- V0.7.0 migration present.
- Bulk/views/preferences/export API routes present.
- TS/TSX syntax transpile validation passed in build workspace.
- Full `npm install` could not finish in the artifact environment because npm registry access timed out; run `npm run check` in local/Vercel environment.
