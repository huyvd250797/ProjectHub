# ASC WORKING V1.3.0 — Validation

- `npm run preflight`: 178/178 PASS.
- TypeScript syntax transpile: 114 TS/TSX files PASS bằng TypeScript compiler API.
- `npm install --ignore-scripts --no-audit --no-fund` đã thử nhưng npm registry timeout trong môi trường đóng gói, vì vậy full `npm run build` chưa thể chạy tại đây.
- Không đóng gói `node_modules`, `.env.local`, `tsconfig.tsbuildinfo`.
- V1.2.0 Analytics migration trong source đã là bản sửa cú pháp `FILTER`.
