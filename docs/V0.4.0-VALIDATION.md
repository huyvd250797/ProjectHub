# V0.4.0 Validation

Checks performed on the generated source:

- TypeScript/TSX syntax parse: 46 files, 0 syntax errors.
- `package.json`, `tsconfig.json`, `vercel.json`: valid JSON.
- Native `<select>` tags in TS/TSX: 0. Project Switcher uses themed custom combobox.
- App logo and `app/icon.jpg` SHA-256 match exactly.
- No `.env.local` packaged; only `.env.example` exists.
- V0.4.0 migration included: `202608220003_v040_contract_rpc.sql`.

Full `npm install` / `next build` could not be completed in the build sandbox because package registry installation timed out. Run `npm install && npm run check` locally or let Vercel perform the install/build.
