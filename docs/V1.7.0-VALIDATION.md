# ASC WORKING V1.7.0 — Validation

## Automated

- `npm run preflight`
- `npm run typecheck`
- `npm run lint -- --quiet`
- `NEXT_TELEMETRY_DISABLED=1 npm run build`

## Expected checks

- Package version là `1.7.0`.
- Health endpoint trả release `Plan Execution & Tracking`.
- Plan API load được `tasks` và `checklistItems`.
- Task API có validate owner/stage cùng Project.
- Checklist API có validate milestone cùng Project.
- Migration V1.7.0 tạo bảng, RLS, trigger validate và activity log.

## Runtime smoke test

- `/plan` render thành công.
- `/api/plan?projectId=...` trả về `tasks`, `checklistItems`, `summary.executionProgress`.
- `/api/health` có feature `plan-execution-tasks`, `milestone-checklists`, `execution-dashboard`.

## Regression cần giữ

- Stage thủ công vẫn giữ nguyên Từ ngày/Đến ngày khi tính lại timeline.
- Stage auto vẫn nối lịch sau stage thủ công.
- Milestone cũ vẫn hoạt động và không bắt buộc phải có checklist.
- Export CSV cũ vẫn có Master Plan, Project Stages và Milestones.
