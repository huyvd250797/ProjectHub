# ASC WORKING V1.4.0 — Validation

Ngày kiểm tra: 27/08/2026.

## Kết quả tự động

| Kiểm tra | Kết quả |
|---|---|
| V1.4.0 preflight | 285/285 pass |
| TypeScript `tsc --noEmit` | Pass |
| ESLint | Pass, 0 error; cảnh báo legacy vẫn được báo cáo |
| Next.js production build | Pass |
| Route generation | `/documents` + 5 API Project Documents được tạo |
| Demo smoke | `/api/health` trả V1.4.0; `/api/documents` trả 3 tài liệu demo; `/documents` HTTP 200 |

## Kiểm tra bảo mật tĩnh

- Google Client Secret và Refresh Token không có giá trị trong source.
- Google credential chỉ được đọc từ environment server-only.
- Browser không nhận access token; chỉ nhận resumable session URI.
- Hoàn tất upload phải qua session ID, one-time token hash, user ID, thời hạn, Project role và xác minh Drive metadata.
- File content API kiểm tra login, Project access và trạng thái archive trước khi proxy.

## Điều kiện chưa thể xác minh offline

Upload thật lên Google Drive, RLS trên Supabase production và Activity runtime cần environment/credential thật. Thực hiện checklist `UAT_V140_PROJECT_DOCUMENTS_CHECKLIST.md` sau khi chạy migration và cấu hình OAuth.
