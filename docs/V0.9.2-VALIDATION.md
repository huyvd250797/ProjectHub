# ASC WORKING V0.9.2 — Validation

Validation performed while packaging:
- Preflight: 22/22 PASS.
- TS/TSX syntax transpile: 95 files, 0 syntax errors.
- Targeted strict type-check for canonical import parser/template passed with external-module stubs.
- JSON/package version checked: 0.9.2.
- `.env.local`, `tsconfig.tsbuildinfo`, `node_modules` not packaged.
- V0.9.2 migration, template route, preview/apply route and docs included.
- RESOURCE canonical parser rejects headers matching password/pass/secret/token.
- Production Apply requires Project code confirmation and MASTER/Admin/PM server-side access.
- Full `npm install && npm run build` could not be completed in the artifact environment because npm registry access timed out; Vercel should perform the full production build.
